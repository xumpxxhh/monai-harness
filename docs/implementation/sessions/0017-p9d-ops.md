# Session 0017 — P9d 运维收口

| 项 | 值 |
| --- | --- |
| 日期 | 2026-08-28 |
| 类型 | 实现 |
| HANDOFF 已更新 | 是 |

## 目标

P9d：harness 角色开关、L1-on-PG、engineering EDR-007 文档一致。

## 改动

- `apps/harness`：`HARNESS_ROLES` / `HARNESS_ROLE_*`；loops 按角色 tick；governance 角色关则不审计 Pack 注册
- `persistence-postgres`：`postgres-l1-loop.test.ts`（CreateRun→running / 双投递 / 补偿）；L2 recovery 补传 `manifestStore`
- `docs/engineering`：README / 02 / 04 将 EDR-007 标为 Accepted（Hono）

## 验证

```text
pnpm --filter harness test
pnpm --filter @monai/persistence-postgres test
```

## 未完成

- 阶段 A 不因 P9 完成而关闭（Token/cost 价表仍缺）
