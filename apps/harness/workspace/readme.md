# Harness Workspace

This directory is the agent workspace for `pnpm --filter harness demo` / `serve`.

You can add files here; `workspace_list` / `workspace_read` / `workspace_search` / `workspace_write` operate on this folder.
