# Harness Workspace

This directory is the agent workspace for `pnpm --filter harness demo` / `serve`.

You can add files here; `workspace.list` / `workspace.read` / `workspace.search` / `workspace.write` operate on this folder.
